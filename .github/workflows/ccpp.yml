name: ci
on: [push]
jobs:
  ci:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@master

    - name: install dependencies
      run: make deps

    - name: build
      run: make release

    - name: run tests
      run: make tests
